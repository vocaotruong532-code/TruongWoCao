name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 👉 Các package cần thiết
  provider: ^6.0.5
  shared_preferences: ^2.2.2
  audioplayers: ^6.1.0
  image_sequence_animator: ^2.0.0   # 🔥 để animate sprite sheet

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    # 🎵 Thư mục chứa âm thanh
    - assets/audio/
    # 🌄 Thư mục nền
    - assets/backgrounds/
    # 🃏 Thư mục hình thẻ
    - assets/cards/
